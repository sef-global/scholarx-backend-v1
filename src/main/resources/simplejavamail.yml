simplejavamail:
  smtp:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL}
    password: ${APP_PASSWORD}
  defaults:
    from:
      name: Sustainable Education Foundation
      address: ${EMAIL}
